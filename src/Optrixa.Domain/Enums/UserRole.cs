namespace Optrixa.Domain.Enums;

// These are stored as strings in the AspNetRoles table via Identity
public static class UserRoles
{
    public const string Admin    = "Admin";
    public const string Employee = "Employee";
}